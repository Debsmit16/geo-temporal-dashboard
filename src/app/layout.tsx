import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Geo-Temporal Weather Dashboard",
  description: "Interactive weather data visualization with intelligent polygon mapping and real-time data integration",
  keywords: ["weather", "dashboard", "visualization", "mapping", "temporal", "data"],
  authors: [{ name: "Debsmit Ghosh" }],
  openGraph: {
    title: "Geo-Temporal Weather Dashboard",
    description: "Interactive weather data visualization with intelligent polygon mapping",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
